# Directory where Excel files will be stored
UPLOAD_DIR=./data

# EXACTLY 32 characters, e.g. generated by: python -c "import secrets; print(secrets.token_hex(16))"
UPLOAD_TOKEN=0123456789abcdef0123456789abcdef

# Max request size in MB (across all files in a single POST)
MAX_CONTENT_LENGTH_MB=50

# Flask port (only used when running app.py directly)
PORT=5000

# Base URL of your Flask API (no trailing slash)
UPLOAD_API_BASE=https://comophodk.aiims.edu/

# EXACTLY 32 characters (must match the server's .env)
# Generate: python -c "import secrets; print(secrets.token_hex(16))"
UPLOAD_TOKEN=0123456789abcdef0123456789abcdef

# Folder containing Excel files to uploadssss
EXCEL_DIR=D:\comophdata\002 PEC\01 Final Data\All CSV files

# Try to keep each request at or below this size to avoid 413 errors
MAX_BATCH_MB=40

# HTTP request timeout (seconds)
REQUEST_TIMEOUT=300
